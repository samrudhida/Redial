"""
app/agents/
───────────
LangGraph agent definitions.
Each agent is a stateful graph that orchestrates LLM calls and tool use.

Example agents:
  agents/retry_decision_agent.py  — decides whether/when to retry a mandate
  agents/compliance_agent.py      — checks retry against RBI regulations
"""
